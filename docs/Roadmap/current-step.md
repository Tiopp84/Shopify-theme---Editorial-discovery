# Current step — Điểm tiếp tục của dự án

> Tệp này là nguồn trạng thái ngắn gọn để tiếp tục dự án giữa các phiên làm việc. Sau mỗi thay đổi đã được kiểm tra, cập nhật mục **Đã hoàn thành**, **Bằng chứng** và **Việc tiếp theo**. Không đánh dấu hoàn thành nếu chưa đạt tiêu chí nghiệm thu tương ứng trong roadmap.

## Checklist bắt buộc khi bắt đầu mỗi phiên

1. Đọc trạng thái, blocker và **Việc tiếp theo** trong tệp này; kiểm tra Git status trước khi sửa.
2. Với mọi flow tương tác nhiều bước hoặc async, đọc và áp dụng [`../Specifications/interaction-architecture-standard.md`](../Specifications/interaction-architecture-standard.md) trước khi thiết kế UI/code.
3. Xác định trước state, controller ownership, render boundary, Shopify ownership, URL/history, continuity, async/concurrency, fallback và lifecycle; không triển khai theo từng bản vá rời rạc.
4. Với mọi thay đổi layout, áp dụng ba khoảng Compact `<48rem`, Tablet `48–63.99rem`, Desktop `≥64rem` theo [`foundation-architecture.md#responsive-layout-contract`](../Specifications/foundation-architecture.md#responsive-layout-contract); không tạo breakpoint/hành vi trung gian không có contract.
5. Kiểm tra cả initial render và trạng thái sau interaction/async DOM replacement ở viewport mục tiêu.
6. Chỉ cập nhật gate/evidence sau khi các kiểm tra tương ứng thực sự pass.

## Trạng thái hiện tại

| Trường | Giá trị |
|---|---|
| Cập nhật lần cuối | 2026-08-06 (Asia/Ho_Chi_Minh) |
| Phase | Phase 8 — Secondary templates |
| Milestone | M0 — ACHIEVED; M1 — ACHIEVED (visual polish deferred); M2 — ACHIEVED |
| Trạng thái tổng thể | IN PROGRESS |
| Release readiness | NOT READY |
| Roadmap nguồn | `docs/Roadmap/01-product-and-eligibility.md` |

## Mục tiêu đang thực hiện

Triển khai Phase 8 Secondary templates: hoàn thiện và kiểm chứng các template phụ với dữ liệu thật/rỗng, giữ Shopify là nguồn sự thật và không mở rộng scope ngoài roadmap đã khóa.

## Đã hoàn thành

- [x] Tạo bộ roadmap từ product strategy đến final release gate.
- [x] Xác nhận trạng thái ban đầu: `theme/` và `tests/` chưa có implementation production.
- [x] Thiết lập tệp `current-step.md` làm điểm tiếp tục xuyên phiên.
- [x] Khởi tạo Git repository với nhánh `main` và tạo baseline commit cho toàn bộ roadmap.
- [x] Hoàn thành market scan định hướng ban đầu và lập product brief đề xuất để phê duyệt.
- [x] Product brief được phê duyệt; English-only v1.0 với localization contract từ đầu.
- [x] Hoàn thành desk research competitive analysis 15 theme và xác định năm khoảng trống thị trường.
- [x] Hoàn thành basic naming screen; loại Loomline/Atelier khỏi public naming và chọn Narrivelle làm provisional lead.
- [x] Narrivelle được chấp thuận làm commercial name provisional; professional clearance vẫn là release/public-brand gate.
- [x] Khóa source provenance, dependency và asset license policy cho Skeleton import.
- [x] Khóa MVP feature matrix với priority, role owner, acceptance và explicit out-of-scope.
- [x] Import official Shopify Skeleton Theme HEAD `a4f32d3` bằng Shopify CLI 4.5.1.
- [x] Chạy baseline Theme Check: 39 files, zero offenses.
- [x] Lưu Skeleton baseline trong commit riêng `7fdb6a1` cùng provenance/license evidence.
- [x] Refactor tài liệu: Roadmap chỉ còn 12 tệp điều hành; discovery/specification/governance được tách riêng.
- [x] Loại `theme/shopify.theme.toml` khỏi Git để không commit development store URL.
- [x] Baseline storefront và Theme Editor smoke test trên development store đã pass theo xác nhận của chủ dự án.
- [x] Tạo clickable prototype Phase 1 cho home → collection → product → cart và mobile navigation, kèm state reviewer và Shopify architecture map.
- [x] Owner chấp nhận prototype làm hướng thiết kế để chuyển sang foundation; visual polish không bị coi là final UI.
- [x] Khóa foundation architecture, token roles, typography settings, semantic main/skip link, focus và reduced-motion primitives.
- [x] Hoàn thiện foundation price/pagination snippets và button/form/visually-hidden primitives; áp dụng vào product, collection và search baseline.
- [x] Hoàn thiện responsive image và original icon primitives; bổ sung accessible navigation/account/cart labels cho header baseline.
- [x] Review Phase 2 exit criteria: clean development-store render, local validator, Theme Check và CI đều đạt; cho phép chuyển sang Phase 3.

## Đang thực hiện

- [x] PDP gallery thumbnail overflow/performance refinement: Product section now exposes `Thumbnail position` when its gallery uses thumbnails, with `Below gallery` as the default and `Gallery side` for a desktop-only vertical rail. The side rail is left of media when Desktop media position is left and automatically moves to the media's right edge when it is right; compact/tablet retain the horizontal rail below media. Horizontal/vertical rails show a directional gradient-arrow cue only where further thumbnails are hidden; cues update after scroll and resize, and clean up with the gallery lifecycle. The initially selected image is the only eagerly loaded image; remaining gallery images use native lazy loading, while fixed-size thumbnail buttons retain a neutral loading surface. Theme Check pass: 81 files, zero offenses; repository validator and JavaScript syntax pass; `git diff --check` pass, 2026-08-06. Shopify Preview needs a long media set at compact/tablet/desktop, both media/thumbnail position combinations, keyboard focus and rapid rail-scroll verification.

- [x] Thêm Home-only `Scrolling text` trong `Campaign & editorial`: section body độc lập với 1–6 Scrolling message blocks và optional link. Merchant có Text alignment, Message spacing (8–80 px), Scroll speed (1–20), Bar height (28–120 px), Background color và Text color; default spacing/height giữ dải gọn. Track chỉ render một group, không tự nhân bản; hover/focus pause motion và reduced-motion dừng animation. Header-owned Announcement bar không bị sửa. Theme Check pass: 81 files, zero offenses; `git diff --check` pass, 2026-08-06. Shopify Preview cần xác nhận Add/remove/reorder, linked/unlinked message, keyboard, reduced motion và long text tại compact/tablet/desktop.

- [x] Mở Announcement bar như một Add section riêng ở body Home, nhưng giữ nguyên canonical Header-group instance global. Schema nay cho phép `groups: ["header"]` và `templates: ["index"]`, có Home-only `Forms & utility` preset với một Announcement block mặc định; section body không tải anchor runtime nên không cố định lên Header hoặc thay đổi header offset. `limit: 1` áp dụng cho mỗi placement surface. Theme Check pass: 80 files, zero offenses; `git diff --check` pass, 2026-08-06. Shopify Preview cần xác nhận Header không đổi, Home picker/add-remove-save-reload, body bar in-flow, keyboard và motion/no-JavaScript fallback.

- [x] Thêm `Email sign up` thành standalone `Forms & utility` section với native Shopify customer form, `newsletter` customer tag, server success/error và no-JavaScript fallback. Section có preset và `enabled_on.templates: ["*"]`, nên Add section hiển thị trên mọi JSON template được Shopify hỗ trợ (không áp dụng cho Liquid Gift card template); `limit: 1` giữ một signup surface có chủ đích trên mỗi template. Footer-owned Newsletter block vẫn là signup cố định trong Footer; hai surface không trùng UI nhưng cùng đưa subscriber vào Shopify audience/tag. Theme Check pass: 80 files, zero offenses; `git diff --check` pass, 2026-08-06. Shopify Preview cần xác nhận add/remove/save/reload, submit success/error, keyboard/zoom và compact form stacking.

- [x] Contact form được mở thành một Home/Page section có preset `Forms & utility`, với `limit: 1` để mỗi template chỉ có một native Shopify contact form. `page.contact` giữ canonical instance; Home và các Page template khác có thể Add section form một cách có chủ đích. Ở editorial layout desktop `≥64rem`, heading/intro là CSS-sticky với safe-area offset và chỉ có scroll travel khi native form dài hơn; compact/tablet và centered layout giữ normal document flow. Form POST, server success/error và no-JavaScript fallback không đổi. Theme Check pass: 79 files, zero offenses; `git diff --check` pass, 2026-08-06. Shopify Preview cần xác nhận Add/remove/save/reload, limit trên `page.contact`, long intro/form, keyboard, zoom và sticky behavior desktop.

- [x] Thêm Home-only `Editorial details` vào `Campaign & editorial`: section có 2–4 internal Detail blocks để diễn đạt Material, Proportion, Care hoặc Delivery mà không lặp Product blocks trên PDP. Merchant chọn một treatment chung Image hoặc Icon cho toàn bộ set; editor chỉ hiện image picker + Image ratio (Square, 4:5, 3:4, 3:2 hoặc 2:1) khi Image được chọn, hoặc grouped icon picker + section-level Icon color/Icon size (48–128 px, mặc định 72 px) khi Icon được chọn. Icon picker dùng native grouped select cho Shopping & payments, Shipping & delivery, Communication, Trust & quality và Craft & editorial; màu và size icon thuộc section gốc, áp dụng đồng nhất cho mọi card. Icon không có frame/border, và section không có eyebrow. Icon mode self-host 20 Phosphor Regular SVG `v2.0.8` as CSS masks, with MIT notice and asset-register evidence; no icon font, CDN, package or runtime is added. Không có runtime JavaScript; storefront bỏ block thiếu heading, Theme Editor visual preview có placeholder. Responsive contract là một cột compact, hai cột tablet và 2/3/4 cột desktop; all customer/schema copy locale-backed. Cập nhật picker taxonomy. Theme Check pass: 79 files, zero offenses; `git diff --check` pass, 2026-08-06. Repository validator hiện bị chặn bởi lỗi sẵn có `search-page-suggestions.liquid` thiếu `enabled_on`/`disabled_on`; Shopify Preview/Theme Editor add-remove-reorder, long copy, missing media và 320/375/768/1024/desktop evidence còn pending.

- [x] Cart behavior global setting: thêm `Cart type` với Drawer mặc định và Cart page là native fallback, cùng lựa chọn Cart icon Bag (default), Cart hoặc Basket. Hai icon surface Header desktop và mobile task bar đều render cùng lựa chọn; Bag giữ nguyên hình icon trước refactor. Product-form nay tự ownership submit-time inventory/remaining-quantity validation cho cả hai mode: sau khi pass nó re-submit native form có marker một lần. Drawer consume marker, skip request inventory trùng rồi AJAX add/mở dialog; Cart page không mount Drawer nên native Shopify submit/điều hướng sang Cart page. Validation fail luôn giữ shopper ở PDP và hiện lỗi như Drawer. Cart page `Show order note`, button/price typography và cart page behavior khác giữ nguyên. Theme Check pass: 77 files, zero offenses, 2026-08-06. Shopify preview cần xác nhận Drawer/Page at desktop, compact và add-to-cart success, inventory limit/error, unavailable/network-error.

- [x] Cart order note cleanup: `Show order note` chuyển từ Cart page section sang Global Cart, nên cùng bật/tắt ở Cart page và Drawer. Drawer nay dùng CartStore note debounce/persistence có sẵn, gồm textarea, saving/saved/error live status và fallback HTML khi drawer phải render lại client-side; không còn hidden note input không tương tác. Cart page giữ đúng field và state cũ, chỉ đọc global setting. Theme Check pass: 77 files, zero offenses, 2026-08-06. Shopify preview cần xác nhận saved setting migration, rapid note edits, reopen/hydrate drawer và Cart page tại compact/tablet/desktop.

- [x] Form field token cleanup: Contact name/email/phone inputs và message textarea bỏ local 1 px border, zero radius, 3.25 rem height/padding để dùng `--field-theme-border-width`, `--field-theme-radius`, `--field-theme-height` và primitive padding. Predictive Search drawer wrapper cũng dùng border width/radius global; Order note đã kế thừa primitive field tokens và chỉ giữ textarea min-height theo nội dung. Contact submit đã dùng Button tokens nên không đổi. Theme Check pass: 77 files, zero offenses, 2026-08-06. Shopify preview cần xác nhận Field height/border/radius settings ở Contact, Cart note và Search drawer trong mọi breakpoint/focus state.

- [x] Search results hierarchy remediation: `/search` không còn trộn Product card, Page text-only và Article media card trong một grid nên không tạo ô trống hay row-height vỡ. Paginated results được tách theo cùng taxonomy với Predictive Search: Products giữ product tile grid/columns/ratio của Search section; Collections, Pages và Articles thành các group text-list hai cột (một cột compact) có heading/divider/link target nhất quán. Search heading dùng commerce font/scale thay vì campaign serif 192 px và khoảng cách tới result count/grid được nén lại. Standard GET search, pagination và no-JavaScript navigation được giữ nguyên. Theme Check pass: 77 files, zero offenses, 2026-08-06. Shopify preview cần xác nhận mixed resource results, product-only, text-only, no-result, pagination và long title ở compact/tablet/desktop.

- [x] Link button color cascade fix: `a { color: currentcolor; }` có specificity cao hơn generic button rule đặt trong `:where(...)`, khiến link button 404 kế thừa foreground tối trên nền button tối dù global Button text color là sáng. Button primitive giờ có `a.button { color: var(--button-color); }`, nên toàn bộ link mang class `.button` dùng token global mà không phải có ngoại lệ 404. Theme Check pass: 77 files, zero offenses, 2026-08-06. Shopify preview cần xác nhận 404 và Cart empty-state link button ở compact/tablet/desktop.

- [x] Product tile/grid ownership cleanup: Theme Settings chỉ có Product tiles, sở hữu media fit, vendor, sale/sold-out badge, second image, title font/size/400–700 weight/letter spacing/one-two-line clamp, cùng price font/size. Các ID font/price hiện có được giữ nguyên. PDP title chỉ kế thừa tile **font family**; PDP vẫn sở hữu title scale/weight/spacing và purchase composition. Collection, Search và PDP Recommendations lần lượt sở hữu image ratio và grid columns của chúng; Recommendations có tablet/desktop column controls, Search có desktop/mobile column controls, còn Collection giữ grid/facet/Quick Add controls. Tile CSS vẫn clamp title và card hẹp vẫn giữ behavior price-wrap. JSON parse, duplicate-setting audit và Theme Check pass: 77 files, zero offenses, 2026-08-06. Shopify preview cần xác nhận Theme Editor persistence, grid settings và title/price hierarchy ở Collection, Search, Recommendations, long title/no media/sale/sold-out tại compact/tablet/desktop.

- [x] Collection tile cleanup: Global `Collection tiles` sở hữu chung nguồn ảnh (Collection image hoặc First product image), image size/ratio, font và size title, card style (text below/overlay/overlay box) và overlay text position cho fixed Collections grid (`/collections`) và addable Collection list (Home/404). Hai section bỏ local `image_source`, `image_ratio`, `item_style`, `item_text_position` và `card_title_font_size`; Collection List không còn ép campaign font và cả card thật lẫn Theme Editor placeholder dùng global tile settings. Hai section tiếp tục tự sở hữu cột, gap, heading/intro và heading alignment; page-level Collection heading không dùng token tile. Thêm translation `Collection title` cho header nhóm setting. Theme Check pass: 77 files, zero offenses, 2026-08-06. Shopify preview cần xác nhận saved Collection List cũ nhận token global, including wide-media four-column fallback, và title/media hierarchy ở compact/tablet/desktop.

- [x] Brand global settings: thêm optional display brand name và favicon. Display name chỉ thay Header/Footer/Password fallback và `og:site_name`; `shop.name` vẫn là nguồn title SEO. Social sharing image giữ nguyên Shopify-native trong Online Store Preferences: `page_image` đã là ảnh Shopify dùng cho search/social preview, nên theme không tạo setting trùng lặp. Favicon render một `rel="icon"` link khi merchant upload asset. JSON parse và Theme Check pass: 77 files, zero offenses, 2026-08-06. Shopify preview cần xác nhận fallback/override và preview social metadata.

- [x] Global layout setting cleanup: loại merchant-facing `max_page_width` và `min_page_margin`; thay bằng hai primitive cố định `--layout-content-max: 90rem` và `--layout-gutter: var(--space-5)`. Toàn bộ tham chiếu `--page-width`, `--page-margin`, `--page-gutter` và các `90rem` cùng vai trò đã được chuẩn hóa về hai token này; nhờ đó các surface từng dùng `--page-gutter` không còn có padding vô hiệu. Theme Check pass: 77 files, zero offenses, 2026-08-06. Shopify preview/Theme Editor responsive evidence cho fixed layout contract vẫn cần ghi nhận.

- [x] Collections grid (`/collections`) refactor: fixed `collections.liquid` section sở hữu heading + rich text intro, position của cả intro và text alignment độc lập; grid sở hữu 2/3/4 items desktop, spacing, image ratio và nguồn media Collection image/First product image. Card bỏ CTA `View collection` vì toàn card vẫn là destination link; style section-owned gồm Text below image, Text overlay, Text overlay with box, cùng vị trí text overlay 9 điểm. Addable `collection-list.liquid` trên Home/404 không bị thay đổi ngoài exception 404 đã có. Static gate pass: `git diff --check`, repository validator (106 files, 172 storefront keys, 349 schema keys) và Theme Check (75 files, zero offenses), 2026-08-04. Shopify preview needs source-image fallback, all text positions/styles and collection inventory layout evidence at compact/tablet/desktop.

- [x] Collection template được tách thành hai fixed section trong thứ tự `Collection header` → `Collection product grid`. Header chỉ sở hữu full-width collection image/banner, height slider, 0–80% overlay slider, title position/alignment và text colour; không còn beside/text-only layout selector hay color scheme. Breadcrumb global trên Collection có spacing dưới riêng trước banner; các template khác giữ spacing cũ. Product grid giữ nguyên `collection-discovery` controller/section-rendering boundary, nên filter, sort, pagination, URL/history, loading/live status và focus continuity không re-render header. Grid nay sở hữu description đầu results + alignment, enable collection count, desktop Sidebar/Drawer + enable filtering/sorting, products per row × rows per page (mặc định 4 × 5 = 20), image ratio/second-image, Quick view toggle + merchant button label và một `Show variants` toggle cho cả swatch/variant links. Collection story insertion và position controls đã bị loại. Swatch/variant links dùng Shopify variant URL nên có destination/no-JS fallback, không có client state mới. Static gate pass: `git diff --check`, repository validator và Theme Check, 2026-08-04. Shopify preview needs settings persistence, filter/sort/pagination after split, empty collection description and representative swatch/variant fixtures at compact/tablet/desktop.

- [x] Product Card hierarchy remediation: card index is now a restrained Moss editorial marker, vendor remains secondary, and the product decision is no longer a title/price two-column race. Title uses a compact two-line commerce scale and price sits below it with consistent left alignment, preserving sale/unit-price semantics and the existing quick-add/media behavior. `git diff --check`, repository validator (104 files, 172 storefront keys, 338 schema keys) and Theme Check (74 files, zero offenses) pass, 2026-08-04. Collection/search/recommendation preview at compact/tablet/desktop, long-title and missing-media evidence pending.

- [x] 404 recovery discovery exception: không tạo section mới; `Featured edit` và `Collection list` hiện có thêm template `404` vào `enabled_on`, nên merchant có thể dùng Add section để đặt một curated product collection hoặc collection links dưới fixed 404 recovery action, rồi add/remove/reorder như Home. 404, Password và Gift card vẫn không trở thành generic builder; exception được ghi rõ trong section-picker taxonomy. `git diff --check`, repository validator (104 files, 172 storefront keys, 338 schema keys) và Theme Check (74 files, zero offenses) pass, 2026-08-04. Shopify Theme Editor 404 add/remove/reorder and missing-resource evidence pending.

- [x] PDP section/block ownership cleanup: `product.liquid` chỉ còn sở hữu gallery layout, media position/ratio, thumbnail behavior và colour scheme; Vendor, SKU và Availability chỉ render khi merchant giữ block tương ứng trong Product information; native Quantity luôn thuộc Product Form thay vì bị section toggle che. Size guide Page chuyển từ Product section vào Product Form block, nên link/modal/fallback đi cùng purchase flow mà không tạo shared hidden state. Schema được nhóm Layout → Style ở section, Content ở form block; legacy Product visibility keys bị loại. JavaScript syntax, `git diff --check`, repository validator (104 files, 166 storefront keys, 338 schema keys) và Theme Check (74 files, zero offenses) pass, 2026-08-03. Shopify preview, existing-setting migration and Theme Editor block reorder evidence remain pending.

- [x] Product Block Add-block visual preview: khi Shopify render picker không có `product`/variant thật, Title, Price, Vendor, SKU, Availability, Product Form, Description và Reassurance hiển thị placeholder có scope `request.visual_preview_mode`; storefront không có product data tiếp tục omit sạch, không fake product claim. Mọi fallback root vẫn mang `block.shopify_attributes`; Form preview là static, không khởi tạo controller/mutation. JavaScript syntax, `git diff --check`, repository validator (104 files, 171 storefront keys, 338 schema keys) và Theme Check (74 files, zero offenses) pass, 2026-08-03. Shopify Theme Editor visual-preview evidence pending.

- [x] PDP Product information stack: mỗi Product Block có wrapper class theo role để `product.liquid` quản lý cadence thay vì các margin rời rạc. Vendor/Title/Price/SKU/Availability tạo một decision group ngắn; Availability có khoảng chuyển sang Product Form; Form tách rõ trước Description; Description được mở đầu bằng divider; consecutive Details disclosures không double border. Reassurance nay luôn có accessible name kể cả khi merchant chỉ nhập content/page mà không đặt heading. JavaScript syntax, `git diff --check`, repository validator (104 files, 172 storefront keys, 338 schema keys) và Theme Check (74 files, zero offenses) pass, 2026-08-03. Shopify preview at 320/375/768/1024/desktop and Theme Editor reorder evidence pending.

- [x] PDP decision hierarchy visual remediation: Product title and price scale now lead the commerce column; price receives a single low-contrast divider before SKU/availability; Product Form remains visually integrated with the page and begins after its own thin divider rather than a tonal panel; description starts after a separate divider and confidence/disclosure blocks stay below it. The treatment uses the existing mineral-neutral roles, Ink primary CTA and 8/16/24 px cadence; it does not change product data, form POST, variant state or JavaScript ownership. JavaScript syntax, `git diff --check`, repository validator (104 files, 172 storefront keys, 338 schema keys) and Theme Check (74 files, zero offenses) pass, 2026-08-04. Shopify preview visual/accessibility evidence pending.

- [x] Phase 8 recovery vertical slice: `404.liquid` nay là semantic recovery composition với heading, giải thích và CTA về all-products; `password.liquid` giữ native `storefront_password` form, server-rendered errors, labelled required input và fallback merchant message; `gift_card.liquid` vẫn là standalone native Gift Card template, hiển thị balance, disabled/expired and expiry status, logo/card fallback, formatted code và Apple Wallet URL khi Shopify cung cấp. Không thêm JavaScript hay merchant blocks. `git diff --check`, repository validator (104 files, 166 storefront keys, 340 schema keys) và Theme Check (74 files, zero offenses) pass, 2026-08-03. Shopify-preview data/state and responsive evidence remain pending.

- [x] Phase 8 Blog/Article/List collections static remediation: Blog có empty state locale-backed thay vì một grid trống; List collections có empty state locale-backed thay vì khoảng trống; `related-stories` nay được gắn cố định sau Article trong `article.json` và bỏ preset để không xuất hiện như section addable trái ownership taxonomy. Không thêm JavaScript, customer state hay merchant control. `git diff --check`, repository validator (106 files, 176 storefront keys, 349 schema keys) và Theme Check (75 files, zero offenses) pass, 2026-08-04. Shopify preview vẫn cần kiểm tra Blog/Article/List collections với dữ liệu thật/rỗng, missing image, long copy và lifecycle.

- [x] Shoppable hero hotspot dùng native click/touch disclosure (`details[name]`) làm no-JavaScript baseline: click lần hai đóng preview và một section chỉ có một preview mở. Progressive enhancement riêng `shoppable-hero-hotspots.js` mở ngay khi hover chuột, nhưng chỉ đóng sau 180 ms kể từ lúc rời hotspot; quay lại hotspot trong thời gian đó hủy close để người dùng đi qua preview không bị nháy. Click ngoài hotspot đóng mọi preview và blur focus còn nằm trong hotspot. CSS chỉ reveal trạng thái `[open]`, nên focus ring keyboard không giữ preview/visual active sau khi close. JavaScript syntax và static verification pass, 2026-08-03.

- [x] Shoppable hero mở rộng product preview ở desktop `≥64rem`: khung tăng tối đa từ 12rem lên 17rem, media từ 4rem lên 5.5rem; tablet và compact giữ khung hiện hữu để tránh che content/hotspot hoặc vượt viewport. Static verification pass, 2026-08-03.

- [x] Shoppable hero mobile viewport guard: mỗi preview mở được đo sau render và chỉ dịch đủ để giữ 12px inset với bốn mép viewport; reset khi đóng và đo lại khi resize. Desktop giữ vị trí art-directed nếu không có nguy cơ tràn. JavaScript syntax và static verification pass, 2026-08-03.

- [x] Quick Add mobile giữ product confidence thay vì ép media vào khung ngang: bỏ cap 18rem khiến stage bị nén/crop; stage dùng portrait 4:5 và ảnh `contain` trên neutral surface để luôn thấy đầy đủ asset. Dialog giữ max-height/scroll nội bộ, nên controls tiếp tục truy cập được sau media. Static verification pass, 2026-08-03.

- [x] Quick Add mobile giới hạn native dialog còn `65dvh` để giữ khoảng thở lớn quanh modal; media, options và CTA dài tiếp tục cuộn nội bộ. Static verification pass, 2026-08-03.

- [x] Announcement bar có một canonical instance `announcement-bar` trong `header-group.json`, gồm Announcement block generic `announcement-1` mặc định; do đó nó hiện đúng trong Header group cùng Header section. Schema chỉ enabled cho Header group, có `limit: 1` và không có preset, nên không tạo duplicate hay entry Add section; merchant dùng Enable announcement để ẩn/hiện thay vì loại bỏ surface. `git diff --check`, repository validator (104 files, 162 storefront keys, 340 schema keys) và Theme Check (74 files, zero offenses) pass, 2026-08-03.

- [x] Bổ sung quy chuẩn Singleton shell sections trong section-picker taxonomy: canonical group JSON instance với ID hyphenated, placement hẹp, `limit: 1`, không preset, generic default blocks và visibility setting thay cho workflow xoá; Announcement bar là ví dụ tham chiếu. `git diff --check`, repository validator (104 files, 162 storefront keys, 340 schema keys) và Theme Check (74 files, zero offenses) pass, 2026-08-03.

- [x] Announcement bar refactor: merchant tạo các Announcement block nhỏ, mỗi block có text + optional link; section không còn text/link/repeat count đơn. Settings được nhóm Content → Motion → Style. Chỉ còn hai motion: Slide and fade dùng track carousel ngang: block hiện tại đi trái, block kế tiếp đi vào từ phải và fade chéo. Khi đến lượt cuối, runtime xoay các node hiện có với transition tắt trong một layout commit, giữ pixel của block đang hiện, rồi tiếp tục trượt trái; không dùng clone hoặc reset nhìn thấy được. Slider Slide speed 1–20 chỉ hiện ở mode này và map 900–210 ms để mức nhanh nhất vẫn nhìn được hành trình carousel, không thành nháy. Continuously scroll chạy phải sang trái, có slider Scroll speed 1–20 riêng (mức 20 hoàn tất một sequence trong 2 giây); local sizing helper tự thêm inert clones vừa đủ cho loop không có khoảng trống/gap lệch theo text và block count, gap lớn hơn và group-end dot đảm bảo separator cả ở clone boundary. No-JS/reduced-motion giữ message đầu tiên. Header group không seed JSON block instance vì Shopify Editor từ chối static group instance trước schema refresh; merchant thêm Announcement block qua editor, không có fallback copy cũ. JavaScript syntax, `git diff --check`, repository validator (104 files, 162 storefront keys, 340 schema keys) và Theme Check (74 files, zero offenses) pass, 2026-08-03.

- [x] Khôi phục Outfit Composition item reveal theo hàng: grid khai báo `data-aos-sequence="row"` cùng early trigger để controller gán delay 0–300 ms từ trái sang phải cho từng hàng. Product item vẫn là các stage độc lập; placeholder Add section không có AOS marker và luôn hiển thị an toàn. Static verification pass, 2026-08-03.

- [x] Outfit Composition nay có section-owned `Show second image on hover` (mặc định bật) trong Layout. Setting được truyền thẳng vào shared `product-card`; `outfit-item` vẫn chỉ sở hữu Product và Title override. Khi sản phẩm có ảnh thứ hai, reveal dùng hover trên fine pointer và keyboard focus parity; tắt setting không render ảnh phụ. Static verification pass, 2026-08-03.

- [ ] Bắt đầu refactor merchant UX theo `Specifications/section-picker-taxonomy.md`: taxonomy picker có `Campaign & editorial` (literal preset category, vì Theme Editor không resolve `t:` tại field này), và `editorial-hero` được tổ chức lại thành Image → Content → Layout → Style. Hero có image desktop/mobile, slider `Text overlay opacity`, text color, heading format/position, slider viewport height riêng desktop/mobile, và CTA colors; các lựa chọn height cũ Compact/Standard/Tall được thay bằng slider mới để không diễn giải khoảng bằng nhãn rời rạc. `Text-only background` bị loại vì không tạo merchant decision rõ ràng; Hero text-only dùng color scheme nền tảng. `Content position` có đủ 9 vị trí trái/giữa/phải × trên/giữa/dưới; Liquid ghi trực tiếp block/inline/text alignment và hướng overlay thành CSS variables trên full-Hero grid parent, nên điều khiển không phụ thuộc class selector. Media/content cùng grid area; content layer stretch theo full hero và Grid row `minmax(selected-height, auto)` giữ long copy an toàn. Repository validator nay kiểm tra đệ quy mọi `t:` key trong schema Section/Theme Block và range defaults theo step, không còn pass giả chỉ vì key không xuất hiện trong Liquid `| t`. Static pass: repository validator (102 files, 157 storefront keys, 279 schema keys), Theme Check (73 files, zero offenses), `git diff --check`, và Shopify CLI đã chấp nhận upload `sections/editorial-hero.liquid` cùng `locales/en.default.schema.json` lên development theme `#190375493996`, 2026-08-01. Shopify preview, Theme Editor persistence/lifecycle, long copy, missing image và 320/375/768/1024/desktop evidence vẫn pending.

- [x] Phase 7 — Differentiation được owner tạm chấp nhận hoàn tất ngày 2026-07-31. Editorial hero, shoppable story và outfit composition đã có vertical slice theo contract; các Shopify-preview, Theme Editor và fixture evidence chưa có vẫn được giữ là regression/deferred work, không chặn việc bắt đầu Phase 8.

- [x] Phase 8 Page/contact/FAQ static vertical slice: `page.liquid` là reading composition server-rendered; `contact-form` dùng Shopify native contact form với success/error POST fallback; `faq` dùng native `details`/`summary`; `page.contact` và `page.faq` là alternate templates rõ ràng. Không có JavaScript/controller mới. Static gate pass: validator (102 files, 157 storefront keys, 232 schema keys), Theme Check (73 files, zero offenses) và `git diff --check`, 2026-07-31. Shopify preview, Theme Editor và real/empty-data evidence vẫn pending.

- [x] Article được giản lược về một composition cố định: hierarchy title/context → media hỗ trợ → body; sidebar chỉ có Author và chỉ render khi profile được nhập, không còn layout/alignment toggle hay product surface trong Article. Dưới Article, section `related-stories` hiển thị tối đa bốn bài khác từ cùng blog, card ảnh/title/date và không commerce interaction. Sau khi owner xóa section type cũ trong Theme Editor, source đã đổi hẳn từ `shop-the-story` sang `related-stories`; không còn compatibility schema/type. Related grid dùng track 14–16rem và ảnh 3:2, nên 1–4 bài đều cân đối. Static gate pass: validator (98 files, 149 storefront keys, 226 schema keys), Theme Check (69 files, zero offenses) và `git diff --check`, 2026-07-31; Theme Editor/lifecycle evidence vẫn pending.

- [x] Article không còn ép feature image vào crop `16:9`: mặc định `Original image ratio` giữ nguyên tỷ lệ asset blog; merchant chỉ chọn Portrait `4:5` hoặc Landscape `16:9` khi chủ đích crop, và media có thể tắt hoàn toàn. Composition Article ưu tiên hierarchy đọc: Blog link → title/context → media hỗ trợ max 22rem → body trong cột 42rem; text luôn nhận foreground của scheme. Article cũng có color scheme và heading left/center. Settings đều Article-owned, presentation chỉ hiện khi media bật và không thêm runtime. Static gate pass: validator (98 files, 150 storefront keys, 234 schema keys), Theme Check (69 files, zero offenses) và `git diff --check`, 2026-07-31; Theme Editor/lifecycle evidence vẫn pending.

- [x] Curated product/content grids (Featured Edit, Outfit composition, Related stories và Blog Compact) dùng opt-in incomplete-row centering: card giữ đúng bề rộng cột tại breakpoint hiện hữu, nhưng hàng cuối thiếu item được căn giữa. Catalog Collection/Search vẫn căn trái để filter/pagination không làm kết quả nhảy vị trí; không thêm setting hoặc runtime. Static gate pass: validator (98 files, 147 storefront keys, 217 schema keys), Theme Check (69 files, zero offenses) và `git diff --check`, 2026-07-31; responsive visual evidence vẫn pending.

- [x] `Shop the story` đã được thay thế bằng `Related stories` bên dưới Article: render các bài khác cùng blog thay vì product cards. Tên file/type được giữ để existing section instance không bị mất, còn product blocks cũ chỉ là compatibility shim và không render. Theme Editor/lifecycle evidence vẫn pending.

- [x] Blog listing nay có setting thuộc riêng Blog template: `Editorial` mặc định hiển thị các bài theo nhịp đọc rộng rãi, với thumbnail media nhỏ bên trái để title/excerpt là trọng tâm; còn `Compact` thành grid 2 cột tablet / 3 cột desktop. Author, publication date và computed reading time là ba checkbox riêng. Merchant có thể bật/tắt toàn bộ article media; khi bật, chọn khung Square/Portrait/Landscape, với Portrait `4:5` mặc định cho editorial/fashion imagery và setting khung chỉ hiện khi media bật. Mỗi card có fallback placeholder khi thiếu ảnh, omit metadata author khi trống và omit excerpt khi không có nội dung; server-rendered, không runtime/animation controller, không thể xuất hiện ngoài Blog template. Static gate pass: validator (97 files, 146 storefront keys, 215 schema keys), Theme Check (68 files, zero offenses) và `git diff --check`, 2026-07-31. Owner smoke test sơ bộ xác nhận Blog controls render được; persistence after reload, long content và missing-data lifecycle evidence vẫn pending.

- [x] Thêm hai module placement-safe: `product-reassurance` là Theme Block chỉ được Product section allowlist, render heading/rich text và optional Shopify Page link, đồng thời omit toàn bộ khi content/page trống; Article reading context là Article-owned settings với author, published date và computed read time theo từng checkbox. Article content vẫn server-rendered cố định, nên merchant không thể reorder/loại bỏ bài viết khi chỉnh context. Không thêm runtime. Static gate pass: validator (97 files, 146 storefront keys, 211 schema keys), Theme Check (68 files, zero offenses) và `git diff --check`, 2026-07-31. Theme Editor/long article/missing author evidence vẫn pending.

- [x] Refactor placement governance cho section/block: mọi Liquid section giờ bắt buộc có `enabled_on` hoặc `disabled_on`, được repository validator kiểm tra ở mỗi lần thêm mới; Header/Footer chỉ vào section group tương ứng, static commerce/content sections chỉ vào template owner, các editorial sections (bao gồm Pinned Visual Story) chỉ vào `index`, Product recommendations chỉ vào `product`. Announcement bar giữ canonical Header-group singleton và có một preset Home-body độc lập; hai placement không chia sẻ instance hay offset runtime. Generic Custom section cùng các Text/Group builder blocks đã được loại bỏ vì không có instance template và không thuộc một commerce/editorial contract; Product blocks chỉ còn xuất hiện trong Product section và Newsletter/Footer menu chỉ còn trong Footer. Validator, Theme Check và `git diff --check` pass, 2026-07-31; cần smoke test Theme Editor Add section/blocks để xác nhận picker hiển thị đúng trên Shopify preview.

- [x] Mở rộng merchant decisions có chọn lọc cho shell, catalog và PDP: global Primary/Subtle/Contrast schemes và section-scoped inherited colour roles cho các surface nền lớn; defaults Editorial Minimal là Primary `#FAF8F5/#1A1A1A/#8B7355/#E5DFD6`, Subtle `#F0EBE3/#2B2B2B/#A08D6F/#D9D0C3`, Contrast `#1A1A1A/#FAF8F5/#C4A876/#4A4540`. Mỗi scheme có Background và Text color (foreground) riêng; helper section áp trực tiếp cả background lẫn text color, nên text không kế thừa màu cũ sau khi merchant đổi scheme. Announcement bar vẫn có background/text độc lập vì đây là utility accent, không bị bó vào ba scheme. Collection có 1/2 cột compact, vendor/badge/variant-safe Quick Add, header text/beside/banner với text-only fallback; Header có desktop left/center và account/localization/announcement visibility; PDP có thumbnails/stacked gallery, media left/right và sticky product details ở desktop chỉ khi gallery stacked, mà không đổi variant/media ownership; media ratio luôn áp vào gallery frame, không còn bị aspect ratio ảnh gốc ghi đè. Featured Edit có editorial/compact-commerce cùng ratio theo context. Footer preset và tag cloud bị loại khỏi scope/theme editor. Không thêm px/duration/easing control hoặc controller mới. Stacked gallery giữ toàn bộ media server-rendered và variant featured-media chỉ cập nhật selected state, không ẩn media. Static gate pass: JavaScript syntax, validator (96 files, 145 storefront keys, 208 schema keys), Theme Check (67 files, zero offenses) và `git diff --check`, 2026-07-31. Shopify preview và Theme Editor lifecycle evidence cho các setting mới vẫn pending.

- [x] Chốt inventory không dùng Storefront API/token: PDP vẫn chỉ hiển thị Available/Sold out; ngay trước Ajax add, PDP đọc `cart.js` mới nhất và render lại selected variant qua Online Store Section Rendering để kiểm tra `requested ≤ inventory hiện tại − quantity cùng variant trong cart`. Nếu vượt, PDP chặn add, không mở drawer và báo lỗi tại form. Cart vẫn phản hồi local ngay; `cart/change.js`/`cart/add.js` là xác nhận cuối cùng cho race condition tồn kho và rollback về last confirmed khi Shopify từ chối.

- [x] Khóa Cart contract/data matrix và triển khai vertical slice đầu tiên: cart page có no-JS POST fallback cho quantity/remove/note/checkout, empty state, line/cart discounts, selling plan/unit price; cart drawer là native-dialog progressive enhancement với một local state cho quantity/line-total/subtotal. Thay đổi được debounce 650 ms theo line key, sau đó `cart/change.js` xử lý tuần tự từng line để Shopify validate; thay đổi tiếp theo trong lúc request chạy được queue, không abort; request lỗi chỉ rollback giá trị chưa bị user thay đổi lại về last confirmed state. Cart state được aggregate theo `variant_id` và phát sang PDP; tracked deny-oversell PDP cap là inventory trừ quantity variant trong cart, được refresh trước add. Add được xác nhận bằng cart read trước khi drawer mở để không có empty drawer. Có loading/error live status, direct cart fallback và header cart-count refresh. Static gate pass: JavaScript syntax, validator (78 files, 125 storefront keys, 110 schema keys), Theme Check (59 files, zero offenses) và `git diff --check`, 2026-07-24. Browser/Shopify preview, accelerated checkout và Theme Editor evidence vẫn pending.
- [x] Khóa PDP contract và product data matrix cho media image/video/external video/3D, variant single/multi/unavailable, selling plans, quantity/pricing và optional data; triển khai vertical slice gallery + variant source of truth với native product-form fallback. Static gate pass: JavaScript syntax, validator, Theme Check và `git diff --check`, 2026-07-23.
- [x] Owner live PDP checkpoint PASS (tạm thời), 2026-07-24: sale/price, inventory, color/size unavailable, quantity và missing-media placeholder đều render đúng; form mua vẫn usable khi thiếu media. Đây là evidence partial, chưa đóng full PDP browser/Theme Editor gate.
- [x] Tách product-detail merchant UX thành Theme Blocks nhưng giữ nguyên hierarchy/style mặc định: vendor, title, price, SKU, availability, product form (variant/quantity/buy/size guide) và description đều là default blocks theo đúng thứ tự UI cũ; collapsible content là block merchant-addable, không được thêm mặc định. Selling-plan select chỉ render khi Shopify có selling-plan groups; video/external video không autoplay. Static gate pass: validator, Theme Check (57 files, zero offenses) và `git diff --check`, 2026-07-24. Live selling-plan/block/Theme Editor verification vẫn pending.
- [x] Khóa block-targeting policy: Product và Footer dùng schema allowlist explicit, không dùng generic `@theme`; Product giữ `@app` làm integration point. Generic Custom section và Group builder đã được loại bỏ trong cleanup tiếp theo, nên không còn merchant-facing `@theme` surface. Private underscore targeting bị hoãn vì Shopify editor active từ chối private type trong JSON template; static gate pass: validator, Theme Check (57 files, zero offenses) và `git diff --check`, 2026-07-24.
- [x] Thêm Product section settings có giới hạn và default giữ nguyên giao diện: media ratio portrait, thumbnails bật khi có nhiều media, vendor/SKU/availability/quantity bật. Không thêm sticky details vì current single-media layout thường có product information cao hơn gallery, nên setting không tạo giá trị rõ và tăng support burden. Visibility settings do Product section sở hữu qua class CSS để Theme Blocks luôn render đầy đủ node cần cho form/controller; tắt SKU/inventory/quantity không làm hỏng variant form. Static gate pass: JavaScript syntax, validator, Theme Check (57 files, zero offenses) và `git diff --check`, 2026-07-24. Theme Editor/live verification vẫn pending.
- [x] Triển khai Product recommendations/complementary thành Product-template section full-width, không đặt trong cột product details: Shopify Product Recommendations endpoint là source of truth; merchant chọn related/complementary intent, heading và giới hạn 2–8 items. Section chỉ enabled trên product template và có preset nên xuất hiện trong Add section; progressive-enhance từ safe no-JS omission, có loading/error live status, AbortController, stale-request guard và cleanup khi Theme Editor thay section. Static gate pass: JavaScript syntax, validator (76 files, 113 storefront keys, 109 schema keys), Theme Check (58 files, zero offenses) và `git diff --check`, 2026-07-24. Live data, empty/error và Theme Editor lifecycle verification vẫn pending.
- [x] Owner live test Product recommendations PASS, 2026-07-24: section full-width, related/complementary configuration và Theme Editor use case hoạt động đúng. Tạo runbook [`../QA/phase-5-pdp-owner-review.md`](../QA/phase-5-pdp-owner-review.md) cho full PDP gate; empty/error-path evidence vẫn được bao phủ trong gate này.
- [x] Sửa no-JS product-form fallback cho multi-variant product: native Variant selector chỉ xuất hiện khi JavaScript tắt, POST đúng `id` và disable variant sold-out; JavaScript vẫn dùng hidden `id` riêng để không tạo duplicate form field. Video/external video/3D chưa có live fixture nên ghi NOT EVIDENCED, không suy diễn PASS. Static gate pass: JavaScript syntax, validator (76 files, 113 storefront keys, 109 schema keys), Theme Check (58 files, zero offenses) và `git diff --check`, 2026-07-24. Live recheck pending.
- [x] Owner no-JS PDP recheck tạm chấp nhận, 2026-07-24: native Variant selector không cho chọn sold-out và direct sold-out purchase path được chặn. Tiếp tục gate với image gallery/modal, keyboard, responsive và Theme Editor; video/external video/3D vẫn NOT EVIDENCED do chưa có fixture.
- [x] Owner full PDP gate PASS, 2026-07-24: variant, sold-out/no-JS fallback, no-media, image gallery/modal, recommendations, keyboard, responsive và Theme Editor lifecycle đều đạt với fixture hiện có. Selling plans là N/A vì store không có selling-plan groups; video/external video/3D là NOT EVIDENCED vì catalogue chưa có fixture. Đóng Phase 5 theo phạm vi fixture hiện có; các data type chưa evidence phải được retest khi merchant thêm dữ liệu.
- [x] Hoàn thành prototype implementation checkpoint: desktop/mobile responsive, filters/history, variant-safe product decision, cart states và accessible bottom sheets.
- [x] Hoàn thành internal comparison chứng minh khác biệt ở cấp hệ thống so với Dawn/competitor patterns; không dùng palette/font làm bằng chứng.
- [x] Owner duyệt hướng prototype để đóng M1 và chuyển phase; visual/mobile polish tiếp tục tại component reviews.
- [x] Thiết lập CI cho Theme Check, schema/locale validation và scan debug/remote URL; GitHub Actions run PASS.
- [x] Development-theme storefront clean-render pass trên các route chính; semantic/token inspection pass.
- [x] Hoàn tất Theme Editor lifecycle/settings persistence và manual keyboard/mobile/reduced-motion review theo owner confirmation.
- [x] Owner retest page width Narrow/Standard/Wide và console sau remediation hoàn tất.
- [x] Keyboard/focus, true 375/320 và zoom 200% được owner xác nhận hoàn tất.
- [x] Phase 3 account decision: giữ direct Shopify account route; authenticated account/order/address coverage chuyển sang hardening test.
- [x] Khóa Phase 3 global-shell contract và triển khai announcement, responsive header/navigation, native-dialog mobile drawer, search entry, footer Theme Blocks, newsletter và localization controls.
- [x] Manual Theme Editor/keyboard/375/browser smoke test cho global shell trên Shopify preview origin được owner xác nhận PASS.
- [x] Remediate Phase 3 desktop navigation typography regression: menu có type scale riêng, nowrap labels và responsive gap để không vỡ từng từ trong header grid.
- [x] Remediate Phase 3 desktop submenu continuity: sửa invalid spacing token giữa brand/menu; single-open submenu, outside-click close và Escape/focus restoration với native-details fallback.
- [x] Remediate desktop compact-header surface: `section-color-scheme` xuất background inline cho Header, nên wrapper `.header-shell--compact` nay ép `background` và `background-color` trong suốt bằng `!important`; thanh navigation nhỏ bên trong giữ background/viền riêng hiện có. Không thay đổi Hero, Liquid data, controller hoặc responsive tablet/compact ownership. `git diff --check`, repository validator (106 files, 176 storefront keys, 349 schema keys) và Theme Check (75 files, zero offenses) pass, 2026-08-04. Shopify preview desktop scroll evidence vẫn pending.
- [x] Khóa Phase 4 discovery contract; triển khai shared product-card cho collection/search với stable ratio, price/sale/sold-out semantics, missing-media fallback và focus=hover parity.
- [x] Product-card live edge-state review: sold-out, missing media và representative long title PASS ở desktop/375/320 theo owner evidence 2026-07-22.
- [x] Product-card visual remediation implemented và owner/live edge-state review PASS: status trên media, metadata nhẹ, title/price decision row, sold-out/missing-media/long-title đúng ở desktop/375/320.
- [x] Triển khai collection filter/sort GET contract, desktop facets, active remove/reset và native-dialog mobile facets; automated preview sort/filter state pass.
- [x] Collection facets browser gate: true 320/375, Escape/backdrop/close focus restore, browser Back/Forward, no-results, price boundaries và post-AJAX layout pass 27/27 assertions.
- [x] Remediate collection state composition: sort và checkbox filter desktop/mobile submit ngay; active filters cộng dồn; sort được giữ; empty price params bị loại; browser interaction test pass.
- [x] Owner review collection spacing remediation PASS: compact hero/toolbar, 15rem facet rail, shrink-safe price controls, wider product grid và mobile no-overflow.
- [x] Owner review desktop filter layout setting PASS: Sidebar/left Drawer, collapsed groups, full-width grid, focus behavior và responsive handoff sang mobile drawer.
- [x] Hoàn tất static audit và owner runbook cho Phase 4 edge review; không phát hiện regression có bằng chứng trong product-card/collection code hoặc ảnh review hiện có.
- [x] Remediate drawer continuity: sau auto-filter drawer mở lại, giữ group đang mở và restore focus vào option vừa chọn; true 375 CSS px browser test pass.
- [x] Refactor collection discovery sang một AJAX section-rendering controller: không document reload, GET fallback, URL/history, AbortController/stale guard, loading/error/live status, drawer/group/focus continuity và pagination interception; browser sort/filter/Back/rapid-change pass.
- [x] Triển khai predictive search progressive enhancement: GET fallback, product/collection/page/article rendering contract, debounce, AbortController/stale guard, loading/error/live status, ArrowDown và Escape/focus; browser mobile/error regression pass.
- [x] Thêm Header `Search behavior` setting: điều hướng `/search` hoặc mở search drawer; desktop dùng left drawer, mobile dùng bottom sheet, tái sử dụng predictive controller/form và luôn giữ link tới full search page. Browser gate pass cho focus restore, `sh` trả 4 products, 375 px không overflow và `/search` vẫn là dropdown.
- [x] Remediate predictive-search missing-media live regression: product không có featured image vẫn render placeholder 4:5 trong media column, giữ alignment với các result khác.
- [x] Clamp predictive-search product title còn hai dòng trên mobile drawer; dấu ellipsis giữ result list gọn trong viewport nhỏ mà không thay đổi accessible text/destination.
- [x] Owner retest predictive missing-media, mobile title clamp và search-query casing PASS, 2026-07-22.
- [x] Owner/live-data Article fixture PASS: predictive search render đúng nhóm Articles, destination hợp lệ và không overflow ở desktop/375, 2026-07-22.
- [x] Đóng Phase 4 Discovery: filter → product flow, collection merchandising, product-card edge states, search/predictive search type coverage và error/empty/mobile gates đều đạt.
- [x] Triển khai collection banner/media và story insertion trong product grid: nội dung story lấy theo từng collection qua `custom.collection_story` metaobject reference, không tạo product giả, không thay đổi Shopify product count/filter/pagination semantics; validator và Theme Check pass.
- [x] Owner/live-data smoke pass collection story, 2026-07-22: entry metaobject riêng được gán và render đúng theo collection; collection không gán không hiển thị; filter/sort/pagination và product-count semantics không regression.

- [x] Audit trực tiếp demo Tier 1 trên home/collection/product/cart/mobile navigation trước design gate.
- [x] Hoàn tất structural live-demo notes cho Palo Alto/SoMa và Blum/Celia.
- [x] Hoàn tất structural live-demo notes cho Pipeline/Clean và Stiletto/Stiletto.
- [x] Hoàn tất structural live-demo notes cho Sleek/Glossy và Concept/Tech; đủ 7/7 Tier 1 themes.
- [x] Hoàn tất owner spot-check cho Stiletto, Pipeline và Concept.
- [x] Nhận và lưu Stiletto owner spot-check: mobile quick view/cart gọn, ngắn nhưng đủ thông tin.
- [x] Nhận và lưu Pipeline owner spot-check: cart bình thường; hover quick-add sizes hữu ích.
- [x] Nhận và lưu Concept owner spot-check: mobile UI tối giản, bottom task bar và bottom sheets hiệu quả.
- [x] Chuyển findings Tier 1 thành năm measurable design principles và prototype acceptance criteria.
- [x] Review exit criteria 01–02; Phase 0 đủ điều kiện chuyển sang Phase 1.
- [x] Thiết lập single-owner plan, workstream owner và target gate dates trong `09-project-board.md`.

## Quyết định đã chốt

- Merchant mục tiêu ban đầu: fashion/apparel/lifestyle.
- Số preset cho bản phát hành đầu tiên: 2.
- SLA support dự kiến: phản hồi trong 2 business days.
- Không sử dụng Dawn, Horizon hoặc commercial theme khác làm source code cho bản submission.
- Product: editorial discovery cho fashion/lifestyle catalog medium–large (100–2.000 sản phẩm).
- Giá launch: USD 350; xem xét USD 400 sau validation.
- Source baseline: Shopify Skeleton Theme, implementation và art direction nguyên bản.
- Locale v1.0: English-only nhưng mọi storefront/schema copy phải dùng translation keys; locale expansion là yêu cầu kiến trúc ngay từ đầu.
- Phase 0 được đóng ngày 2026-07-19; Phase 1 bắt đầu bằng prototype, không viết component production trước design gate.

## Quyết định còn thiếu

- Professional trademark clearance cho commercial theme/preset names.
- Owner và target date cho từng workstream.

## Blocker và rủi ro hiện tại

- Professional trademark clearance vẫn bắt buộc trước public brand/listing assets; không chặn prototype nội bộ.
- Demo assets mới vẫn bị chặn cho đến khi có license/provenance trong register.
- Yêu cầu Shopify Theme Store có thể thay đổi; phải đối chiếu tài liệu chính thức tại đầu Phase 0, trước beta và trong tuần submission.

## Bằng chứng

| Hạng mục | Vị trí | Kết quả |
|---|---|---|
| Roadmap dự án | `docs/Roadmap/README.md` đến `10-release-gate.md` | Có đủ khung phase/gate |
| Trạng thái implementation | `theme/`, `tests/` | Chưa có file production/test |
| Điểm tiếp tục | `docs/Roadmap/current-step.md` | Đã thiết lập |
| Git baseline | Commit `978cf11` trên nhánh `main` | Roadmap đã được lưu từ root commit |
| Product brief | `docs/Discovery/product-brief.md` | APPROVED — 2026-07-17 |
| Competitive analysis | `docs/Discovery/competitive-analysis.md` | 15 theme; desk research complete 2026-07-17 |
| Tier 1 live-demo audit | `docs/Discovery/tier-1-live-demo-audit.md` | COMPLETE FOR PHASE 0 — 7/7 structural + 3/3 spot-checks |
| Tier 1 interaction checklist | `docs/Discovery/tier-1-interaction-checklist.md` | COMPLETE — Stiletto, Pipeline, Concept |
| Stiletto owner spot-check | `docs/Discovery/evidence/tier-1/stiletto/` | COMPLETE — product quick view + populated cart |
| Pipeline owner spot-check | `docs/Discovery/evidence/tier-1/pipeline/` | COMPLETE — product-card quick add + unavailable size |
| Concept owner spot-check | `docs/Discovery/evidence/tier-1/concept/` | COMPLETE — header/task bar + cart bottom sheet |
| Naming screen | `docs/Discovery/naming-audit.md` | Basic screen complete; Narrivelle provisional, legal clearance pending |
| Provenance/license plan | `docs/Governance/source-provenance-and-licenses.md`, `docs/Governance/asset-license-register.md`, root `THIRD_PARTY_NOTICES.md` | APPROVED FOR FOUNDATION IMPORT |
| MVP scope | `docs/Specifications/mvp-feature-matrix.md` | LOCKED — priority/owner/acceptance/out-of-scope defined |
| Skeleton baseline | `theme/`, upstream `a4f32d393b9eadf6c4403318ca39116832e5d1df`, commit `7fdb6a1` | 39 files inspected; Theme Check zero offenses |
| Documentation structure | `docs/README.md` | Roadmap/Discovery/Specifications/Governance separated |
| Dev-store baseline | Storefront + Theme Editor smoke test | PASS — owner confirmation, 2026-07-17 |
| Design principles/prototype gate | `docs/Specifications/design-principles-and-prototype-criteria.md` | APPROVED FOR PHASE 0 — 2026-07-18 |
| Phase 0 exit review/workstream plan | `docs/Roadmap/01-product-and-eligibility.md`, `02-scope-and-feature-matrix.md`, `09-project-board.md` | PASS — Phase 1 begins 2026-07-19 |
| Phase 1 core-flow prototype checkpoint | `docs/Prototype/index.html`, `prototype.css`, `prototype.js`, `prototype-map.md` | COMPLETE — syntax/diff checks pass; 4 screens, 3 dialogs, state reviewer and architecture map; owner review pending |
| Prototype differentiation comparison | `docs/Prototype/design-comparison.md` | INTERNAL COMPLETE — journal structure, numbered story grammar, variant-safe actions and commerce-first cart; owner approval pending |
| Desktop visual QA | `docs/Prototype/review-home-1440.png` | PASS WITH FIX — initial route focus scroll corrected; header link styling clarified |
| Mobile visual QA attempt | `docs/Prototype/review-home-375.png`, `review-home-320.png` | INCONCLUSIVE — Chrome headless enforces a wider layout viewport then crops output; true 320/375 CSS viewport review still required |
| M1 owner decision | Owner confirmation, 2026-07-20 | APPROVED FOR FOUNDATION — prototype defines direction/behavior, not final pixel UI |
| Foundation architecture and tokens | `docs/Specifications/foundation-architecture.md`, `theme/config/settings_schema.json`, `theme/snippets/css-variables.liquid`, `theme/assets/critical.css`, `theme/layout/theme.liquid` | STARTED — JSON parse pass; Theme Check 39 files, zero offenses |
| Foundation CI | `.github/workflows/theme-ci.yml`, `scripts/validate-theme.mjs` | PASS — GitHub Actions `Theme CI` owner-confirmed, 2026-07-20; validator and Theme Check successful |
| CI Node 24 remediation | `.github/workflows/theme-ci.yml` | PASS — checkout/setup-node v6 and Node 24; local Node 24.14 and GitHub rerun successful |
| Foundation package | `theme/Skeleton-0.1.0.zip` (Git-ignored build artifact) | PASS — Shopify CLI packaged 39 uploadable files; no `shopify.theme.toml` or credential file in archive |
| Development-store smoke runbook | `docs/Specifications/foundation-smoke-test.md` | COMPLETE — storefront automated checks and owner Theme Editor/keyboard/mobile/reduced-motion review pass |
| Development-store clean render | `docs/Specifications/foundation-smoke-test.md`, `docs/Specifications/foundation-home-1440.png` | PASS — core routes render, 404 behaves correctly, no detected Liquid error, semantic/token checks pass |
| Foundation commerce primitives | `theme/snippets/price.liquid`, `pagination.liquid`, `theme/assets/critical.css`, product/collection/search sections | PASS — Theme Check 41 files zero offenses; live product/collection/search render HTTP 200 with price markup and no Liquid error |
| Foundation media/icon primitives | `theme/snippets/image.liquid`, `icon.liquid`, `theme/sections/header.liquid` | PASS — Theme Check 42 files zero offenses; live preview confirms srcset, lazy loading, labelled navigation and inline account/cart icons |
| Owner foundation review | `docs/Specifications/foundation-smoke-test.md` | PASS — settings/layout/skip link/editor lifecycle, keyboard/focus, 375/320, zoom 200% and reduced-motion review complete |
| Owner remediation retest | `docs/Specifications/foundation-smoke-test.md` | PASS — page-width difference visible and console remediation confirmed, 2026-07-20; account popover functionality explicitly deferred to Phase 3 |
| Phase 2 exit review | `docs/Roadmap/03-architecture-and-engineering.md`, `docs/Roadmap/05-build-roadmap.md` | PASS — validator 55 files/43 storefront keys/64 schema keys; Theme Check 42 files zero offenses; clean render and CI evidence complete, 2026-07-20 |
| Phase 3 global shell slice | `docs/Specifications/global-shell-contract.md`, `theme/sections/announcement-bar.liquid`, `header.liquid`, `footer.liquid`, `theme/blocks/footer-menu.liquid`, `newsletter.liquid`, `theme/assets/header-shell.js` | PASS — validator 59 files/55 storefront keys/72 schema keys; Theme Check 45 files zero offenses; preview render and owner interaction smoke pass, 2026-07-20 |
| Phase 4 product-card slice | `docs/Specifications/discovery-contract.md`, `docs/QA/phase-4-owner-edge-review.md`, `theme/snippets/product-card.liquid`, `theme/sections/collection-product-grid.liquid`, `search.liquid` | OWNER/LIVE-DATA PASS — sold-out semantics, linked missing-media fallback and long-title wrapping pass at desktop/375/320; collection spacing and Sidebar/Drawer behavior approved 2026-07-22. Visual refinement 2026-07-28: title is the primary decision signal, reserves two lines and clamps overflow with an ellipsis while the full accessible name remains present; prices align at the decision-row top. |
| Phase 4 collection facets | `theme/snippets/facets.liquid`, `theme/assets/collection-facets.js`, `theme/sections/collection-product-grid.liquid`, `docs/Specifications/collection-facets-review-1440.png` | BROWSER GATE PASS — 27/27 assertions at 1440/375/320 cover sort, cumulative filter continuity, Back/Forward, no-results, price bounds, drawer width, Escape/backdrop/close focus restore, no overflow and post-AJAX layout; Theme Check zero offenses |
| Phase 4 search/predictive search | `theme/sections/search.liquid`, `predictive-search.liquid`, `theme/assets/predictive-search.js`, `docs/Specifications/discovery-contract.md` | OWNER/BROWSER PASS — endpoint/search fallback HTTP 200; rapid typing, missing-media placeholder, mobile title clamp, query casing, 375 no-overflow, ArrowDown, Escape/focus and blocked-network error announcement pass; live Products/Collections/Pages/Articles all observed |
| Phase 4 collection banner/story insertion | `theme/sections/collection-header.liquid`, `theme/sections/collection-product-grid.liquid`, `theme/locales/en.default.schema.json` | RETIRED — collection header/banner is now a dedicated fixed section; the former `custom.collection_story` insertion and its merchant controls were removed during Collection grid refactor. |
| Phase 4 exit review | `docs/Specifications/discovery-contract.md`, `docs/QA/phase-4-owner-edge-review.md`, `docs/Roadmap/05-build-roadmap.md` | PASS — discovery gate closed with complete product/collection/page/article search coverage, filter → product flow, error/empty states and owner mobile evidence; validator 65 files/89 storefront keys/90 schema keys; Theme Check 49 files zero offenses; 2026-07-22 |
| Phase 5 live PDP checkpoint | Owner screenshots, 2026-07-24 | PARTIAL PASS — sale/price, inventory, unavailable size, quantity and missing-media placeholder observed; media-type matrix, URL/no-JS, gallery modal, selling plans, lifecycle and full browser gate remain open |
| Phase 5 merchant UX slice | `theme/sections/product.liquid`, `theme/blocks/product-*.liquid`, `theme/templates/product.json` | STATIC PASS — vendor/title/price/SKU/availability/form/description are default Theme Blocks in the legacy UI order; collapsible content remains optional; native selling-plan fallback added; validator 74 files/111 storefront keys/99 schema keys; Theme Check 57 files zero offenses; live verification pending, 2026-07-24 |
| Phase 5 recommendations slice | `theme/sections/product-recommendations.liquid`, `theme/assets/product-recommendations.js`, `theme/templates/product.json` | OWNER/LIVE PASS — full-width product-template section, related/complementary intent, merchant heading/limit and Add section preset working; static validation 76 files/113 storefront keys/109 schema keys; Theme Check 58 files zero offenses; empty/error-path retained for PDP browser gate, 2026-07-24 |
| Phase 5 PDP exit | `docs/QA/phase-5-pdp-owner-review.md`, owner live testing, 2026-07-24 | PASS FOR AVAILABLE FIXTURES — variant/no-JS, no-media, image modal, recommendations, keyboard, responsive and Theme Editor lifecycle pass. Selling plans N/A; video/external video/3D NOT EVIDENCED and must be retested when data exists. |
| Phase 6 Cart vertical slice | `docs/Specifications/cart-contract.md`, `theme/sections/cart.liquid`, `cart-drawer.liquid`, `theme/assets/cart.js`, `docs/QA/phase-6-cart-owner-review.md` | CLOSED FOR CURRENT FIXTURES — CART-01 drawer is OWNER/LIVE PASS; CART-02 page is OWNER/PREVIEW SMOKE PASS. PDP → drawer, rapid quantity/remove, cart page + note, standard checkout, no-JS fallback and Theme Editor lifecycle all PASS; Console has no error. Owner accepts closure on 2026-07-27. Accelerated checkout is NOT EVIDENCED because the active Test payment gateway supplies no accelerated button; inventory preflight is NOT EVIDENCED because no tracked deny-oversell fixture exists. Both remain deferred regression rows. Validator 78 files/134 storefront keys/110 schema keys; Theme Check 59 files zero offenses. |
| Phase 7 differentiation architecture | `docs/Specifications/differentiation-contract.md` | READY — locks three sections: editorial hero, shoppable story and outfit composition. Each keeps Shopify as product/price/availability/cart authority, uses no-JS links as the baseline, avoids generic builders and fake bundles, and has explicit lifecycle, missing-resource and variant-safe acceptance. |
| Visual design tokens | `docs/Specifications/visual-design-tokens.md`, `stitch_fashion_cart_redesign/narrivelle_homepage_desktop_full_width_hero/`, `stitch_fashion_cart_redesign/narrivelle_homepage_mobile_editorial_overlay/` | FOUNDATION TOKENS IMPLEMENTED — approved palette, sharp controls, semantic spacing aliases, commerce/campaign roles and tabular prices are shared primitives; homepage composition remains scoped to Phase 7. Shopify preview remains the visual acceptance authority. |
| Typography editor foundation | `docs/Specifications/visual-design-tokens.md`, `theme/config/settings_schema.json`, `theme/snippets/css-variables.liquid`, `theme/assets/critical.css` | STATIC PASS — Typography exposes Headings, Body text and Small labels controls without Capitalize. Small labels supplies product form fields, metadata, breadcrumbs, filter/sort group labels, filter launch controls, Contact/Quick add/Cart labels, FAQ numerals and footer headings; filter option values remain Body text for readability. Global `Button & form fields` is split into Button CTA (existing colours plus font, text size, letter spacing, height, horizontal padding, border width and corner radius) and Form fields (height, border width and corner radius); fields inherit Body text to avoid duplicate typography settings. Button CTA settings feed all text CTA including Hero, Shoppable Hero, Page, Material & craft, Contact, Product form, Quick add, Cart, Cart drawer and Newsletter. Quantity controls are explicitly component-owned with fixed 6px outer rounding and square inner controls, so they do not inherit Form fields rounding; icon and swatch controls remain component-owned. Standard Newsletter and Quick add fields, plus the desktop Collection Sort picker, consume global field height, border width and corner radius; at narrow footer widths Newsletter keeps its inline form by proportionally reducing only its CTA padding, then stacks only below 13rem. The sort option popup, compact localization and large Cart textarea retain their component layout. `Product prices` supplies a global font and base price size to the standard price snippet, Product page, Product card, Quick add, Cart and Cart drawer, while editorial product compositions retain their local size overrides. Heading size ranges 24–90 px; standard page and commerce titles, including FAQ and Size guide, consume Headings. Page/About retains its Small/Medium/Large composition control, with each choice derived from Headings. Validator (109 files, 187 storefront keys, 419 schema keys), Theme Check (77 files, zero offenses) and `git diff --check` pass, 2026-08-05. |
| Footer editorial restyle | `theme/sections/footer.liquid`, `theme/blocks/footer-menu.liquid`, `theme/blocks/newsletter.liquid`, `theme/assets/footer-panel.js`, `theme/layout/theme.liquid`, `theme/locales/en.default.schema.json` | STATIC PASS — merchant-owned newsletter/menu blocks remain the content source within a compact editorial dark-footer layout: optional brand mark/description, tag cloud, social navigation and legal links form the upper/lower hierarchy. An unassigned footer menu does not render or reserve a column; the three-column fallback keeps brand, newsletter and tags balanced. At 56 rem it becomes a balanced two-column layout; at 40 rem every footer region and block uses one minmax(0, 1fr) column, preventing the no-menu desktop rule from overflowing mobile. Theme Editor exposes colour, accent, lower-bar colour, logo width, spacing, tag/social visibility, optional newsletter supporting copy, payment icons and an optional page-panel-to-footer transition. In that transition, the footer is fixed behind the page and a matched spacer lets the main panel scroll up to reveal it; scrolling back up moves the panel down over the fixed footer like a closing window. The panel has no additional transform; a 32 px lower-edge radius pulse appears only halfway through overlap and resolves back to a straight edge before max reveal. As it is covered, the footer remains opaque but dims/softens and shifts down by at most 20 px; no panel shadow is used. It uses one rAF-throttled native scroll reader and disables for reduced motion/no-JS. Responsive grid and existing localization/payment functionality remain intact. JavaScript syntax, validator and Theme Check pass; preview/Theme Editor evidence remains pending. |
| Header scroll compact behavior | `theme/sections/header.liquid`, `theme/assets/header-shell.js` | STATIC PASS — with the existing sticky-header setting enabled, crossing 120 px reliably enables a centered, short, content-width desktop taskbar at the top: home brand plus primary menu only; search/account/cart are omitted. It no longer depends on a single scroll frame exceeding 4 px, so slow/trackpad scrolling and restored scroll positions are handled consistently. Pointer-restored focus within the header no longer blocks the compact state; visible keyboard focus and open dialogs keep the header stable for accessibility. Its width and height interpolate into the compact state. The small `›` submenu chevron spins on hover/focus and rests down when open; submenu links have hover/focus background, motion and focus-ring feedback. Submenus render only child links explicitly configured in Shopify Navigation; no synthetic parent “View all” link is added. The full header returns only after a deliberate 120 px upward scroll, avoiding state flicker. Existing menu, submenu, search, cart, keyboard and reduced-motion behavior remain in place. Validator and Theme Check pass, 2026-07-29; Shopify preview evidence remains pending. |
| Mobile/tablet header ↔ task-bar handoff | `theme/sections/header.liquid`, `theme/assets/header-shell.js`, `theme/snippets/icon.liquid` | STATIC PASS — for sticky headers below 64 rem, the Concept-inspired one-hand task bar (Home, Explore, Search, Products, Cart and, when enabled, Account) replaces the top header only after 16 px of deliberate downward scroll beyond 72 px. It remains locked for 900 ms, then requires 32 px of intentional upward scrolling to restore the regular header, avoiding direction-change flicker. Scroll is the sole mode owner: click, focus and native Menu/Search dialog lifecycle preserve the menu bar that was active beforehand. The dialog refocuses its task-bar opener on close; when task bar mode is active, the hidden regular header is inert, and when header mode is active, the hidden task bar is inert. Therefore exactly one actionable menu bar is available at a time. The two surfaces crossfade/translate with CSS transform and opacity only. It is a viewport-fixed, safe-area-aware layer and does not change document/body geometry, protecting existing native drawer anchors. At 48–63.99 rem it is centred and capped at 34 rem rather than stretched across the tablet viewport, keeping every task reachable and visually grouped. No-JavaScript and reduced-motion retain the normal sticky header; menu/search keep their native-dialog, Escape and opener-focus contracts, while cart continues to use the shared cart controller. JavaScript syntax, repository validator, Theme Check error gate and `git diff --check` pass, 2026-07-30. Shopify preview/device verification remains pending at 320, 375 and tablet touch widths, including rapid direction changes, bottom safe area, keyboard focus, menu/search/cart dialogs and Theme Editor lifecycle. |
| Featured edit Theme Editor refactor | `theme/sections/featured-edit.liquid`, `theme/snippets/product-card.liquid`, `theme/locales/en.default.schema.json` | STATIC PASS — the picker is categorized as `Products & collections`; controls are now grouped Content → Products → Layout → Style. Content is intentionally collection plus heading only: eyebrow, description and CTA are removed. Heading size/alignment are explicit. Product count is derived from desktop products-per-row × desktop rows (default 4 × 1), replacing a disconnected product-limit setting; price and index are optional. Layout offers image size and a fine-pointer/keyboard-focus second-image reveal when a second image exists. The selected collection’s View all destination is a fixed, top-right action immediately above the grid. Until a non-empty collection is selected, the section renders a complete, non-interactive Featured collection fallback in both Theme Editor and storefront: heading, View all treatment, four individually framed default Shopify placeholders `product-1` through `product-4`, and generic title/price labels — never store collection/product data. These card controls are opt-in inputs to the shared product-card snippet and do not alter Collection, Search, Product recommendations or Outfit composition. `git diff --check`, repository validator (101 files, 161 storefront keys, 316 schema keys) and Theme Check (73 files, zero offenses) pass, 2026-08-03; Shopify preview, Theme Editor persistence/lifecycle and responsive visual evidence remain pending. |
| Collection list Theme Editor refactor | `theme/sections/collection-list.liquid`, `theme/blocks/collection-card.liquid`, `theme/sections/collections.liquid`, `theme/locales/en.default.schema.json`, `theme/locales/en.default.json`, `docs/Specifications/section-picker-taxonomy.md` | STATIC PASS — Collection list is a distinct, addable `Products & collections` section on Home only; its schema is grouped Content → Layout → Style with heading size/alignment, desktop collection density, image size and scheme. Home has an explicit `collection-card` Theme Block allowlist; the block owns its Shopify collection picker, optional title override (otherwise collection title) and nine-position focal point, while the section owns grid/layout only. Four static `collection-card` blocks are rendered with literal IDs and declared in the section preset using Shopify's `static: true` form, so Add section consistently materializes all four without treating an external Theme Block as a dynamic preset type. Merchants can select a collection in each static block or hide it. Their empty Theme Editor state is four framed Shopify collection placeholders, rendered directly by those blocks without reliance on Shopify-generated block IDs. The block explicitly supports `request.visual_preview_mode`, the path Shopify uses in the Add section picker: card placeholders omit AOS there so preview availability never depends on a homepage runtime, while the section heading retains its normal `fade-down` entrance. The grid uses explicit CSS Grid tracks: two compact/tablet, and exactly three or four at desktop according to the setting; an unset desktop-columns value falls back to four. Each collection card is now a dependent AOS item stage with a 85%-viewport entry trigger, so it fades up before its reserved grid footprint becomes conspicuous, then is observed/cleaned independently. `collections.liquid` is restored as the fixed List collections template owner and always renders the store's complete collection inventory. `git diff --check`, repository validator (103 files, 162 storefront keys, 321 schema keys) and Theme Check (75 files, zero offenses) pass, 2026-08-03; Shopify Theme Editor confirmation remains required for the static-block path. |
| Material & craft Theme Editor refactor | `theme/sections/material-craft.liquid`, `theme/locales/en.default.schema.json` | STATIC PASS — controls are organized Content → Layout → Style. Content now provides heading size and text size alongside the existing paired images, copy and link. Layout provides Text alignment for the whole text frame (eyebrow, heading, body and CTA), its top/centre/bottom position, media side, and image width (40–65%) plus direct image height (360–900 px). Those dimensions drive the desktop/tablet image-to-content grid and paired-image stage. When media is on the right, the collage itself mirrors: the small image stays adjacent to text and the larger image stays at the outside edge. Vertical spacing is removed. The section-picker visual preview omits AOS markers from image and content targets, so fallback placeholders and copy never depend on the homepage animation runtime. |
| Announcement bar controls and marquee | `theme/sections/announcement-bar.liquid`, `theme/locales/en.default.schema.json`, `docs/Specifications/theme-motion-implementation.md` | STATIC PASS — linked copy uses an arrow instead of an underline, and every moving visual copy is clickable; only one is exposed to keyboard/assistive technology. Merchants can set background/text colour, 10–20 px text size and a 28–72 px minimum height, then optionally enable a CSS-only continuous left-to-right marquee with 1–8 evenly spaced repeats. A prominent `·` separates every repeat, including at the loop boundary; named speeds are Slow, Standard or Fast. The marquee pauses for hover/focus and reduced motion shows one stationary, accessible message; no JavaScript runtime is added. Validator and Theme Check pass; preview/Theme Editor evidence remains pending. |
| Homepage editorial choreography | `theme/assets/home-reveal.js`, `theme/sections/pinned-visual-story.liquid`, `docs/Specifications/motion-architecture.md` | STATIC PASS — Pinned Visual Story settings are organized Content → Layout → Motion → Style. Heading alignment changes the text inside its intro frame; Layout positions the whole eyebrow/heading frame left, centre or right. Image width (40–65%) controls the desktop/tablet media-to-text grid ratio and Image height (360–900 px) directly controls the sticky stage; mobile retains its in-flow portrait media. The root Story chapters group controls shared index visibility plus heading size, text size and left/centre/right alignment across every chapter. Vertical spacing is intentionally removed. The desktop story uses one section-scoped GSAP timeline scrubbed through its chapter progression; CSS retains sticky media while GSAP animates only opacity and transform. Mobile, reduced motion and a merchant-disabled setting retain ordinary one-time reveals. The controller uses `gsap.context()` and destroys/re-hydrates cleanly for Theme Editor and breakpoint changes. JavaScript syntax, validator, Theme Check and `git diff --check` pass; Shopify preview/device evidence remains pending. |
| AOS homepage fade experiment | `theme/assets/aos-2.3.4.css`, `aos-home.js`, homepage section markup, `theme/layout/theme.liquid` | IN REVIEW — `feat/AOS` self-hosts AOS 2.3.4 CSS and replaces homepage GSAP loading with first-visible fades: the hero image is visible immediately for LCP, then makes a non-blocking 900ms scale settle while its text uses a horizontal fade; Featured Edit/Outfit card groups use short fade-up stagger; Material, Pinned Story and Shoppable Story alternate directional fades according to their composition. The head bootstrap prepares AOS state before paint, then `aos-home.js` starts only the visible hero text after two render frames. Other homepage sections are considered only after user scroll/resize and queue their first entry at the 60% viewport line in either direction; product grids/items and Pinned Story chapters form a second stage, so they begin after their section lead and only when they themselves reach that line. Reveal duration is AOS-like 400ms, reduced only to 350ms at fast scroll velocity. Visible queued stages use the same 350ms catch-up reveal instead of waiting behind offscreen work, preventing blank viewport content. Child markers are removed after completion. The queue serializes ordinary scrolling stages, so their fades cannot overlap or replay. Pinned Story retains CSS sticky media but no longer runs GSAP scrub on this branch. AOS CSS activates only when reduced motion is not requested; no-JavaScript, blocked-runtime and reduced-motion states retain visible final content. Static syntax/validator/diff checks pass; Shopify preview, real-device performance and Theme Editor lifecycle evidence are pending. |
| Theme motion implementation guide | `docs/Specifications/theme-motion-implementation.md` | ACTIVE REFERENCE — documents the theme-wide motion hierarchy, home runtime, current homepage inventory, pinned-story choreography and review rules. |
| Homepage motion ownership remediation | `theme/assets/aos-home.js`, `aos-overrides.css`, `home-reveal.js`, homepage sections, `docs/Specifications/theme-motion-implementation.md` | STATIC PASS — AOS exclusively owns standard homepage entrances (hero, Featured Edit, Material & craft, Shoppable Story and Outfit Composition); GSAP exclusively owns Pinned Visual Story choreography. The former duplicated transform/opacity ownership and global AOS queue are removed. AOS vectors are reduced to 2 rem; Featured Edit and Outfit Composition cards are independent `fade-up` stages with a 0–300 ms stagger in AOS-supported 100 ms steps; Theme Editor unload clears AOS registrations and timers. JavaScript syntax, repository validator (94 files, 145 storefront keys, 180 schema keys), Theme Check (66 files, zero offenses) and `git diff --check` pass, 2026-07-30. Shopify preview/device/lifecycle verification remains pending. |
| Catalog scroll card reveal | `theme/assets/catalog-reveal.js`, `theme/layout/theme.liquid`, `theme/sections/collection-product-grid.liquid`, `collections.liquid`, `theme/snippets/product-card.liquid`, `theme/assets/collection-facets.js` | STATIC PASS — Collection product cards and List collections cards use AOS `fade-up` once when an `IntersectionObserver` sees each card cross the lower 18% viewport boundary; cards in the same visual row sequence left-to-right with a bounded 0–300 ms delay, without a continuous scroll listener. Search, predictive search and recommendations remain unchanged. Collection's Section Rendering replacement strips the markers before insertion, so filter/sort/pagination results remain visible immediately and never replay. Preview, reduced-motion, no-JavaScript and async replacement verification remain pending. |
| Dialog, modal and drawer motion | `theme/assets/critical.css`, `theme/assets/overlay-motion.js`, header/cart/collection/product sections and `product-card.liquid` | STATIC PASS — native dialogs retain their existing controller/focus/Escape ownership while the lightweight overlay coordinator ensures an observable panel/backdrop fade on open and a short script-initiated exit. Direction follows the surface (side drawer, mobile sheet, centred modal); reduced motion removes the transition. JavaScript syntax, validator, Theme Check and `git diff --check` pass; preview/browser evidence remains pending. |
| Phase 7 editorial hero vertical slice | `theme/sections/editorial-hero.liquid`, `theme/templates/index.json`, `theme/locales/en.default.schema.json` | STATIC PASS — hero is the home template's first section, with one ordinary optional CTA, responsive hero media and a text-only fallback; no JavaScript/runtime is added. Schema has explicit locale-backed labels, desktop/mobile media-height choices and a preset. Validator passes 79 files/134 storefront keys/117 schema keys; Theme Check passes 60 files with zero offenses, 2026-07-27. Shopify preview, 320/375/desktop and Theme Editor lifecycle evidence remain pending. |
| Phase 7 shoppable story vertical slice | `theme/sections/shoppable-story.liquid`, `theme/blocks/story-product.liquid`, `theme/locales/en.default.schema.json` | STATIC PASS — ordered external `story-product` Theme Blocks use independent product destinations, Shopify-derived title/price/availability and an optional editorial image/caption. The block is safe to render externally because it appears once in the story list; unlike Pinned Visual Story, it does not need its data rendered in both a sticky-media and narrative location. The preset creates two dynamic, movable `story-product` blocks; merchants can reorder, delete, duplicate and add further blocks. Missing product blocks are omitted on storefront; no variant is silently added. The section settings are organized Content → Layout → Style: Content offers heading/text size, while Layout offers whole-intro text alignment, left/right text position, adjustable text width (25–45%), product image size and one global Show variants control. The variant setting applies at every breakpoint: mobile keeps compact Colour and Size summaries beneath the price, while desktop uses 22 px swatches. On desktop, the intro stays sticky while long product lists scroll alongside it. Product stages use an 85%-viewport AOS trigger, avoiding late entry for third and later cards. Product cards only render colour dots when every colour value has exact native Shopify swatch data; otherwise, they render `Color: value, value` text. A product Size option renders immediately below Color as `Size: value, value`. Prices reserve additional inset space. Vertical spacing is removed. Empty blocks render a two-card Shopify placeholder preview in the Theme Editor without exposing real store data. Validator passes 104 files/162 storefront keys/337 schema keys; Theme Check passes 76 files with zero offenses, 2026-08-03. Browser/Theme Editor evidence remains pending. |
| Phase 7 outfit composition vertical slice | `theme/sections/outfit-composition.liquid`, `theme/blocks/outfit-item.liquid`, `theme/snippets/product-card.liquid`, `theme/locales/en.default.schema.json` | STATIC PASS — one optional look image/introduction plus independent, movable external `outfit-item` Theme Blocks, rendered by the shared product-card. There is no combined price, bundle claim, grouped cart mutation or product state. The preset starts with three empty movable item blocks, whose Theme Editor fallback uses generic Shopify product placeholders rather than store data. Each item owns only its product picker and optional Title override. The section owns all shared card presentation: 8–48 px item spacing and Show price. Lead settings are organized Content → Layout → Style: heading/text size; whole-text-frame alignment and top/centre/bottom position; media side; image width (30–65%), height (360–900 px), and contain/cover fit; then colours. Eyebrow, heading and body live in one 36 rem maximum copy frame, so left/centre/right moves and aligns the complete copy as one unit rather than allowing intrinsic text widths to drift. Add section preview retains the intro and look-media AOS entrances; only item placeholder cards omit AOS so their visibility never relies on preview runtime timing. Contain is the default, preventing unintentional crop of the full outfit look; Theme Editor renders a lifestyle placeholder when no look image has been chosen. Vertical spacing is removed. Validator and Theme Check pass, 2026-08-03. Browser/Theme Editor evidence remains pending. |
| Tablet responsive architecture | [`docs/Specifications/foundation-architecture.md#responsive-layout-contract`](../Specifications/foundation-architecture.md#responsive-layout-contract), `theme/sections/header.liquid`, `theme/assets/header-shell.js`, `theme/sections/editorial-hero.liquid`, `pinned-visual-story.liquid`, `material-craft.liquid`, `shoppable-story.liquid`, `outfit-composition.liquid`, `collection-header.liquid`, `collection-product-grid.liquid`, `product.liquid`, `theme/assets/home-reveal.js` | STATIC PASS — responsive ownership is now compact `<48rem`, tablet `48–63.99rem`, desktop `≥64rem`. Tablet keeps the header’s accessible menu/search dialogs as side panels and suppresses desktop navigation/compact-scroll behavior; it preserves hero editorial copy, keeps CSS-only Pinned Story media sticky only while its explicit two-column layout remains active, uses non-pinned two-column editorial compositions elsewhere, keeps outfit cards at two columns, uses collection filter drawer plus a three-card grid, and retains a stacked PDP before desktop. GSAP motion choreography starts only at the 64rem desktop boundary. JavaScript syntax, repository validator (91 files, 145 storefront keys, 181 schema keys), Theme Check error gate and `git diff --check` pass, 2026-07-29. Theme Check retains one pre-existing `footer-panel-spacer` scoped-CSS warning. Shopify preview/device and Theme Editor evidence remain pending. |
| Featured Edit grid stabilization | `theme/sections/featured-edit.liquid` | STATIC PASS — the product card no longer relies on `display: contents` or breakpoint-specific grid-area placement. It has one stable card structure (media → metadata → self-contained title/price decision row); the section owns only its active column count: two columns through compact/tablet and merchant-configured three/four columns at desktop `≥64rem`. The former product-card border, inset padding and dashed decorative frame are removed, leaving media flush with the card. The section gutter is now an explicit valid `clamp(1.25rem, 3.3vw, 3.75rem)`: its former `--space-7` reference was undefined and invalidated the whole padding shorthand. Very narrow compact widths stack the price under the title rather than overflow. Preview/Theme Editor verification remains pending. |
| Tablet product-card quick add | `theme/snippets/product-card.liquid` | STATIC PASS — collection/search product cards reserve and show their existing variant-safe quick-add control at the bottom through tablet (`<64rem`), matching compact behavior. Hover-only image/quick-add reveal is now limited to `hover:hover` plus `pointer:fine`; keyboard focus retains the same reveal. Product recommendations and homepage contexts remain excluded by their existing contracts. Repository validator, Theme Check error gate and `git diff --check` pass, 2026-07-29. Preview verification remains pending. |
| PDP gallery direct navigation | `theme/sections/product.liquid`, `theme/assets/product-form.js`, `docs/Specifications/product-detail-contract.md` | STATIC PASS — gallery retains one selected-media state shared by thumbnails, variant featured-media selection, desktop previous/next side controls and horizontal touch swipe. The inline gallery and image-media zoom modal now follow the finger during a horizontal drag; a committed drag slides both media concurrently, while a short drag returns to the current media. Video, external-video and 3D native controls remain excluded in zoom so their own gestures stay usable. Stage arrows are progressive enhancement only and are hidden without JavaScript; normal media click still opens the existing zoom modal. Swipe requires a 48 px predominantly horizontal gesture, while `touch-action: pan-y` preserves vertical page scroll and reduced motion resolves without animation. JavaScript syntax, repository validator, Theme Check error gate and `git diff --check` pass, 2026-07-29. PDP preview verification for image/video/external-video/3D fixtures remains pending. |
| Cart drawer viewport anchoring remediation | `theme/assets/critical.css`, `theme/sections/cart-drawer.liquid` | STATIC PASS — the native cart dialog was incorrectly `position: relative`, so a background scroll could move the panel and expose a bottom gap. It now has a fixed top-layer viewport anchor: desktop uses `inset: 0 0 0 auto`; mobile uses `inset: auto 0 0`; both use `dvh` rather than `svh`. The drawer body contains overscroll and the existing dialog/focus/Escape/backdrop/cart-mutation ownership is unchanged. Repository validator, Theme Check error gate and `git diff --check` pass, 2026-07-29. Preview verification on touch and desktop remains pending. |

## Việc tiếp theo — theo thứ tự

1. Trên Shopify preview, gán `page.contact` và `page.faq` cho hai Page fixtures rồi kiểm tra Page base, Contact success/error/no-JS và FAQ empty/long blocks tại 320/375/768/1024/desktop, keyboard, zoom, reduced motion và Theme Editor lifecycle.
2. Trên Shopify preview, kiểm tra 404, Password và Gift card với dữ liệu/state thực: 404 recovery CTA; Password có/không có store message và native error; Gift card active/disabled/expired/expiry, logo fallback và Apple Wallet khi khả dụng, tại 320/375/768/1024/desktop, keyboard, zoom và reduced motion.
3. Hoàn thiện các secondary template còn thiếu theo inventory tại [`../Specifications/secondary-templates-contract.md`](../Specifications/secondary-templates-contract.md); giữ Blog/Article composition hiện có là điểm xuất phát và không tái sử dụng product/cart controller ở content surface.
4. Trên Shopify preview, chạy Phase 8 gate cho từng template với dữ liệu thật và rỗng, long content, missing image/resource, 320/375/768/1024/desktop, keyboard, zoom, reduced motion và Theme Editor lifecycle. Ghi evidence trước khi đóng Phase 8.
5. Deferred regression: verify homepage motion at desktop, 1024 px, 768 px, 375 px and 320 px; verify cart drawer at desktop, 375 and 320 px after page scroll; and run the Phase 7 visual gate after real hero/story/composition resources are configured.
6. Khi một accelerated checkout provider hợp lệ được bật, chạy và ghi smoke test native handoff; khi có tracked deny-oversell fixture, chạy lại inventory-preflight row của Cart runbook.
7. Khi merchant thêm selling plan, Shopify-hosted/external video hoặc 3D model, chạy lại rows NOT EVIDENCED của PDP runbook.
8. Thực hiện professional trademark clearance trước khi public brand/listing assets; ghi approval/proof trước mọi demo asset hoặc dependency mới.

## Nhật ký phiên làm việc

### 2026-07-17 — Thiết lập dự án

- Đã rà soát toàn bộ `docs/Roadmap`.
- Xác định dự án đang ở Phase 0 và chưa có implementation trong `theme/`/`tests/`.
- Tạo cấu trúc lưu trạng thái xuyên phiên trong tệp này.
- Khởi tạo Git repository trên nhánh `main`.
- Tạo root commit `978cf11` để lưu baseline roadmap và provenance ban đầu.
- Nghiên cứu yêu cầu Theme Store hiện hành và một số đối thủ định hướng trong nhóm fashion/editorial.
- Tạo `docs/Discovery/product-brief.md` với segment, positioning, bốn trụ cột, pricing, locale, source strategy và trade-off.
- Product brief được phê duyệt; cập nhật quyết định chính thức vào tài liệu 01.
- Khóa localization contract: English-only v1.0 nhưng không hard-code UI copy; storefront/schema keys và locale parity là yêu cầu bắt buộc.
- Hoàn thành `docs/Discovery/competitive-analysis.md`: 15 theme, price/preset/positioning/feature/art direction và năm khoảng trống thị trường.
- Xác nhận lookbook/hotspot/swatch/mega-menu/quick-buy là feature parity, không phải differentiation.
- Hoàn thành `docs/Discovery/naming-audit.md`; loại public names Loomline, Atelier và các candidate có xung đột rõ.
- Chọn `Narrivelle` làm provisional lead; giữ Project Loomline chỉ làm codename.
- Chủ dự án tiếp tục dự án với `Narrivelle`; ghi nhận tên provisional đã được chấp thuận.
- Tạo provenance/license plan, asset register và third-party notices baseline.
- Khóa `docs/Specifications/mvp-feature-matrix.md` với các quyết định cart drawer/quick add/predictive search/two presets in scope; recently viewed/voice search/app-like features out.
- Import official Skeleton vào `theme/` qua Shopify CLI; ghi upstream SHA `a4f32d393b9eadf6c4403318ca39116832e5d1df` và license notice.
- Shopify CLI tự nâng từ 4.5.0 lên 4.5.1 trong lúc init.
- Baseline Theme Check pass: 39 files, zero offenses.
- Lưu baseline source và provenance trong commit `7fdb6a1`.
- Refactor `docs/`: giữ Roadmap chỉ cho phase/gate/tracking; chuyển research sang Discovery, scope sang Specifications và provenance/license sang Governance.
- Baseline storefront/Theme Editor smoke test pass theo xác nhận của chủ dự án.
- Bắt đầu Tier 1 live-demo audit; lưu partial evidence cho Prestige/Couture trên home, collection, product và navigation structure.
- Không đánh dấu Prestige complete vì chưa kiểm chứng cart, viewport/mobile interactions và screenshots.
- Hoàn tất structural live-demo notes cho Palo Alto/SoMa và Blum/Celia; cả hai giữ trạng thái partial vì thiếu populated-cart/mobile screenshots.
- Xác nhận outfit/cross-sell UI là parity; differentiation phải nằm ở variant-aware state, product-confidence hierarchy và opinionated merchant defaults.
- Hoàn tất structural live-demo notes cho Pipeline/Clean và Stiletto/Stiletto; cả hai giữ trạng thái partial vì thiếu populated-cart/mobile screenshots.
- Ghi nhận Pipeline là benchmark variant-rich product cards/cart threshold và Stiletto là benchmark product-confidence hierarchy.
- Hoàn tất structural live-demo notes cho Sleek/Glossy và Concept/Tech; Tier 1 đạt 7/7 themes ở mức structural audit.
- Ghi nhận Sleek là benchmark bundle/content breadth và Concept là benchmark mobile task access/variant switching.
- Đơn giản hóa interaction checklist: bỏ deep audit và screenshot bắt buộc vì không phải Theme Store requirement và không đáng chi phí ở Phase 0.
- Chấp nhận structural audit 7/7 là đủ; accessibility và commerce behavior sẽ được test trên chính Narrivelle.
- Next: tạo measurable design principles/prototype acceptance criteria.
- Nhận Stiletto spot-check từ chủ dự án; lưu hai ảnh mobile và kết luận hierarchy gọn, đầy đủ.
- Nhận Pipeline spot-check; progressive-reveal size quick add là pattern nên học nhưng cần mobile/keyboard fallback.
- Nhận Concept spot-check; xác nhận one-hand task bar và bottom-sheet overlays là mobile patterns nên đưa vào design criteria.
- Hoàn tất Tier 1 market audit cho Phase 0; không cần audit thêm đối thủ.
- Chuyển findings thành `docs/Specifications/design-principles-and-prototype-criteria.md`: năm principle đo được, boundaries chống sao chép và prototype gate cho home, collection, product, cart, mobile navigation.
- Review exit criteria 01–02: tất cả điều kiện exit đã có evidence. Đóng Phase 0 và chuyển Phase 1 — Design ngày 2026-07-19.
- Thiết lập kế hoạch một owner trong `09-project-board.md`; các mốc là baseline điều phối, cần điều chỉnh nếu capacity thay đổi.

### 2026-07-19 — Phase 1 core-flow prototype checkpoint

- Tạo prototype độc lập trong `docs/Prototype/`; không thay đổi component production trong `theme/` trước design gate.
- Hoàn thiện flow home → collection → product → cart và mobile task navigation bằng HTML/CSS/JavaScript không dependency, không remote asset.
- Bổ sung state reviewer cho loading, empty, error, unavailable, focus, long copy và missing media.
- Map các vùng prototype sang future Shopify section/block/snippet/custom element trong `prototype-map.md`.
- Kiểm tra JavaScript syntax và `git diff --check` pass; kiểm tra cấu trúc ghi nhận 4 screen, 3 dialog, 3 live region, reduced-motion, URL filter history, delayed cart state và focus restoration.
- Checkpoint implementation hoàn tất; chưa đóng M1 cho đến khi comparison và owner desktop/mobile/accessibility review đạt.
- Hoàn thành `design-comparison.md`: khác biệt được chứng minh bằng journal issue structure, numbered story grammar, variant-safe action language và commerce-first cart; không tuyên bố các pattern parity riêng lẻ là unique.
- Chạy desktop visual QA bằng Chrome headless và sửa lỗi initial route focus làm trang tự cuộn, đồng thời làm rõ header navigation styling.
- Chụp thử mobile 375/320 cho thấy Chrome headless dùng layout viewport tối thiểu rộng hơn rồi crop ảnh; không dùng hai ảnh này làm bằng chứng pass mobile. M1 vẫn mở cho true device-emulation và owner review.

### 2026-07-20 — M1 direction approval và bắt đầu Phase 2

- Owner chấp nhận prototype hiện tại làm hướng thiết kế để chuyển bước; giao diện production không bị ràng buộc pixel-perfect với prototype.
- Đóng M1 cho mục đích foundation; visual/mobile polish được chuyển thành continuous component-review requirement.
- Tạo `foundation-architecture.md` để khóa ownership, CSS/Liquid/localization conventions và Definition of Done.
- Bổ sung body/heading font roles, type scale, accent/border roles, spacing/focus tokens, semantic `main`, skip link và reduced-motion fallback.
- JSON parse pass; Theme Check pass 39 files, zero offenses; `git diff --check` pass.
- Thêm GitHub Actions `theme-ci.yml` theo Shopify CLI CI guidance, pin CLI 4.5.1 và không dùng store credential vì workflow chỉ lint/validate.
- Thêm `scripts/validate-theme.mjs`: parse JSON/JSONC, kiểm tra translation keys được tham chiếu, chặn debug statement và remote JavaScript runtime.
- Local-equivalent CI pass: 52 theme files, 33 storefront keys, 63 schema keys; Theme Check 39 files, zero offenses. GitHub run đầu tiên vẫn chờ commit/push.
- Codex environment từ chối external upload dù owner đã cho phép; không retry hoặc bypass. Local `shopify theme package` pass và tạo Git-ignored `theme/Skeleton-0.1.0.zip` gồm 39 uploadable files, không chứa `shopify.theme.toml`.
- Tạo `foundation-smoke-test.md` với storefront, global-setting, keyboard/reduced-motion và Theme Editor lifecycle checklist để chạy ngoài môi trường này.
- Owner khởi động development theme thành công. Local preview checks pass cho home, collection, product, cart, search, blog, page và list-collections; 404 trả đúng HTTP 404 với rendered body; không phát hiện Liquid error.
- DOM inspection xác nhận `MainContent`, skip link, heading font, accent và type-scale tokens. Lưu desktop clean-render evidence `foundation-home-1440.png`; không commit store URL/theme ID.
- Thêm LiquidDoc price/pagination snippets, compare-at/unit-price semantics, accessible pagination labels và shared button/form/visually-hidden primitives.
- Áp dụng primitives vào product, collection và search Skeleton sections. Validator pass 55 files/41 storefront keys/63 schema keys; Theme Check pass 41 files zero offenses; live preview routes render không có Liquid error.
- Nâng image snippet với alt override, responsive widths/sizes, lazy-loading default và clean missing-image behavior; thêm original inline SVG icon API cho account/cart/menu/search/close.
- Header baseline dùng navigation label và accessible account/cart names. Validator pass 55 files/43 storefront keys/63 schema keys; Theme Check pass 42 files zero offenses; live preview xác nhận markup và không có Liquid error.
- Nhận owner review: typography, undo/redo, margin, colors, save/reload, cross-template persistence, skip link và Theme Editor lifecycle pass; reduced-motion query true; keyboard/mobile/zoom chưa xác định; layout/scale chưa ổn.
- Sửa page-width scale thành 70/90/110rem để tạo khác biệt đo được và thay `shopify-account` bằng account link baseline, loại bỏ nguồn missing-menu GraphQL/localhost Shop CSP/pre-auth errors. Validator và Theme Check vẫn pass; console cần owner retest.
- Owner xác nhận remediation retest hoàn tất: page-width variants nhìn thấy được và Foundation console đạt. Ghi account link là fallback tạm thời; Phase 3 phải quyết định direct link hay account popover và kiểm tra missing-menu/CSP/authenticated account flows.
- Owner xác nhận hoàn tất manual keyboard/focus, true 375/320, zoom 200% và reduced-motion review; Foundation owner-review checklist đạt.
- GitHub Actions run đầu tiên fail tại Theme Check: Shopify CLI 4.5.1 cần `node:module.enableCompileCache`, không có trong Node 20.20.2. Nâng `checkout`/`setup-node` lên v6 và Node job lên 24; local Node 24.14 validation và Theme Check pass 42 files zero offenses, chờ owner commit/push rerun.
- Owner xác nhận GitHub Actions rerun PASS sau Node 24 remediation. Đóng CI exit criterion của Foundation; bước tiếp theo là Phase 2 exit review và chuẩn bị Phase 3 Global shell.

### 2026-08-06 — Predictive search result distribution

- Predictive search dùng quota riêng theo từng resource type (`limit_scope=each`, tối đa 4 mỗi loại), nên Page/Article không thể chiếm hết vị trí khiến Product biến mất với truy vấn ngắn. Cần kiểm tra trên storefront preview với truy vấn một ký tự và query sản phẩm đặc trưng.
- Trang search sau khi submit bổ sung nhóm `Suggested products` từ Predictive Search (chỉ Product, tối đa 4), giữ nguyên `search.results` chuẩn bên dưới và loại các Product trùng lặp. Preview endpoint với `c` xác nhận trả bốn Product card; Theme Check pass 78 files.
- Chuyển Search behavior từ Header section vào global Search settings; bổ sung lựa chọn nội dung predictive dropdown (Products only hoặc Products + content). Header desktop/mobile và predictive form cùng dùng token global; trang Enter vẫn giữ kết quả chuẩn không bị lọc.
